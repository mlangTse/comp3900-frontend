import Layout from '../components/Layout';
import Dish from '../components/Dish';

function DishPage() {
    return (
        <Layout
            body={<Dish />}
        />
    );
}

export default DishPage;